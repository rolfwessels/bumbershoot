name: .NET

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      RedisHost: localhost
    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 3.1.x

      - name: Test
        run: make test

      - name: docker publish
        env:
          SMokker: ${{secrets.DOCKER_USER}}
          Smokker: ${{secrets.DOCKER_PASSWORD}}
        run: |
          make publish
